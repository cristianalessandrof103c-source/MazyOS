# Copie pra .env.local e preencha com os valores do projeto Supabase
# (Project Settings > API no dashboard, ou `supabase status` se estiver rodando local).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
