# Copie este arquivo pra .env (nunca commite o .env de verdade).

# Token gerado uma única vez pelo dashboard (Disparos -> WhatsApp -> Conectar WhatsApp).
# Se perder, não tem como recuperar -- gere um novo no dashboard e cole aqui de novo.
DEVICE_TOKEN=

# URL base das Edge Functions do projeto Supabase (sem barra no final), ex.:
# https://tblumyuozhysncscktrk.supabase.co/functions/v1
FUNCTIONS_URL=
